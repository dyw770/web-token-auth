<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dyw.auth.db.mapper.SysRolePermissionMapper">

    <select id="queryRolePermissions" resultType="cn.dyw.auth.db.domain.SysPermission">
        select
            t3.permission_id,
            t3.permission_desc,
            t3.permission_type,
            t3.create_time
        from sys_role_permission t1 join (
            select descendant_role_code from sys_role_hierarchy
            where ancestor_role_code = #{roleCode}
        ) t2 on t1.role_code = t2.descendant_role_code
        join sys_permission t3
        on t1.permission_id = t3.permission_id
    </select>
    <select id="queryRoleMenuPermissions" resultType="cn.dyw.auth.db.domain.SysPermission">
        select 
            t1.permission_id, t1.permission_desc, t1.permission_type, t1.create_time
            from sys_permission t1 
            join sys_menu_permission t2 on t1.permission_id = t2.permission_id
                 join sys_role_permission t3 on t1.permission_id = t3.permission_id
        where t2.menu_id = #{menuId} and t3.role_code = #{roleCode}
    </select>
</mapper>
