<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dyw.auth.db.mapper.SysPermissionMapper">

    <select id="queryMenuPermissionList" resultType="cn.dyw.auth.db.model.SysMenuPermissionDto">
        select 
            t1.permission_id,
            t1.permission_type,
            t1.permission_desc,
            t1.create_time,
            t2.menu_id
            from sys_permission t1 
            join sys_menu_permission t2 on t1.permission_id = t2.permission_id
        where t2.menu_id = #{menuId}
    </select>
</mapper>
